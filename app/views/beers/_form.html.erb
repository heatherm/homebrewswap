<%= form_for(@beer) do |f| %>
  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being
        saved:</h2>

      <ul>
        <% @beer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </div>
  <div class="field">
    <%= f.label :brewed_on %><br>
    <%= f.datetime_select :brewed_on %>
  </div>
  <div class="field">
    <%= f.label :growlers_remaining %><br>
    <%= f.number_field :growlers_remaining %>
  </div>
  <div class="actions">
    <div class="buttonish alert bordered">
      <%= f.submit %>
    </div>
  </div>
<% end %>
